
export type User = {
    id: string
    name: string
    email: string
    score:number
    type:number
}

export type UserAuth = {
    id: string
    name: string
    email: string
    score: number
    type:number
    token: string
}