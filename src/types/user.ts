export type User = {
    id: number
    name: string
    email: string
    score:number
}

export type UserAuth = {
    id: number
    name: string
    email: string
    score: number
    token: string
}